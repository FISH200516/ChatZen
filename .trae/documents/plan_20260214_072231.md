我将修改对话页（ChatScreen）中的 App Logo 显示样式，使其完全填充圆角矩形区域，去除空白。

**修改计划**

1. **文件**: `app/src/main/java/com/fishai/chatzen/ui/screens/ChatScreen.kt`
2. **定位**: 找到显示 `R.drawable.app_logo` 的 `Image` 组件 (约第 266 行)。
3. **修改**:

   * 添加 `contentScale = ContentScale.Crop` 属性：这会使图片按比例缩放并裁剪，以完全填充容器区域。

**代码变更预览**

```kotlin
Image(
    painter = painterResource(id = R.drawable.app_logo),
    contentDescription = stringResource(R.string.ai_assistant),
    contentScale = ContentScale.Crop, // 添加此行
    modifier = Modifier
        // ... 保持现有 modifier 不变
)
```

