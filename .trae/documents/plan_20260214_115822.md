我将通过缩放（Scale）和裁剪（Clip）的方式来修复图标填充问题。

**原因分析**：
当前显示的空白是由于 Android 默认图标（Adaptive Icon）本身通常带有安全边距（Padding）。简单的 `ContentScale.Crop` 只能裁剪到图片文件的边界，但无法去除图片内部自带的留白。

**修复方案**：

1. **使用 Box 容器**：将 `Image` 放入一个固定大小的 `Box` 中。
2. **应用裁剪与边框**：将圆角裁剪（Clip）和边框（Border）应用到外层的 `Box` 上，确保容器形状正确。
3. **缩放图片**：对内部的 `Image` 应用 `scale(1.5f)`（放大 1.5 倍）。这会将图片放大，从而使中间的图标内容“

