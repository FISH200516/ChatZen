I will implement the Edge-to-Edge adaptation and Deep Thinking content optimization as follows:

### 1. Edge-to-Edge Adaptation (Immersive Navigation Bar)
- **Settings Page (`SettingsScreen.kt`)**:
  - Update the bottom spacing logic. Instead of a fixed `Spacer(100.dp)`, I will apply `WindowInsets.navigationBars` to the `LazyColumn`'s `contentPadding`. This ensures the content can scroll behind the navigation bar while the last item remains accessible above it.
- **Model Selector (`ModelSelector.kt`)**:
  - Modify `ModalBottomSheet` to set `windowInsets` to only include status bars (or zero out bottom insets). This allows the sheet's background to extend behind the system navigation bar.
  - Add `WindowInsets.navigationBars` padding to the `LazyColumn` inside `ModelSelectionContent` to prevent the bottom-most list items from being obscured by the navigation bar.

### 2. Deep Thinking Output Optimization
- **Chat Screen (`ChatScreen.kt`)**:
  - Create a new Composable component `ReasoningContentBlock` to handle the display of "Deep Thinking" content.
  - **Manual Folding**: Add a clickable header (e.g., "Deep Thinking Process ⌄") that allows users to manually expand or collapse the reasoning content.
  - **Auto-Collapse**: Implement logic using `LaunchedEffect` to monitor the streaming status. When the message generation completes (`isStreaming` changes from `true` to `false`), the reasoning block will automatically collapse.
  - **Integration**: Replace the current static text display of `reasoningContent` in `ChatBubble` with this new interactive component.

This approach addresses the UI visual improvements for Android 16 standards and enhances the UX for models with reasoning capabilities.