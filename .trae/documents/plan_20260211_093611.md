根据您的反馈，我更新了计划，特别调整了模型排序的逻辑：

### 1. 交互与导航优化
*   **底部导航栏长按跳转**：
    *   修改 `AppNavigation.kt`，在底部中间的“输入”按钮上添加自定义手势监听。
    *   实现“按住 2 秒以上”才触发跳转 WebView 的逻辑（替换原有的发送按钮长按）。
    *   从 `ChatScreen.kt` 中移除发送按钮的长按事件。

### 2. UI 布局优化
*   **输入框按钮自适应**：
    *   修改 `ChatScreen.kt` 中的输入框下方按钮组（深度思考、联网搜索、图片上传、个性）。
    *   使用权重布局 (`Modifier.weight(1f)`) 确保它们始终在一排显示，并强制文字单行显示（`maxLines = 1`），避免换行。
*   **模型选择列表优化**：
    *   修改 `ModelSelector.kt` 中的 `ModelRow` 组件。
    *   调整布局结构，将视觉标记（Vision/Reasoning 等）显示在模型名称的**上方**，确保视觉标记不会挤压模型名称导致换行。

### 3. 模型管理功能增强
*   **模型启用/禁用控制**：
    *   修改 `ModelInfo.kt`，增加 `enabled` 字段，默认值为 `false`（默认关闭）。
    *   修改 `SettingsViewModel.kt`，实现模型启用状态的保存与更新逻辑。
    *   **模型排序优化**：获取到的模型列表将**优先按名称排序，名称相同时按创建时间排序**。
    *   修改 `ServiceProvidersScreen.kt`，在模型列表中添加开关 (`Switch`)，并增加“一键启用”按钮。
    *   在 `ChatScreen.kt` 的模型选择器中，过滤掉未启用的模型。

### 4. WebView 问题修复
*   **白屏修复**：
    *   修改 `WebViewScreen.kt`，增加 `mixedContentMode` 配置（允许混合内容），并优化 SSL 错误处理和 Cookie 设置，确保网页能正常加载。

我将按照此计划执行。