我将修改 `app/src/main/java/com/fishai/chatzen/ui/screens/ChatScreen.kt` 文件，包含以下两部分修改：

1. **更新功能按钮（深度思考、联网搜索、图片上传、AI个性）的选中状态样式**：

   * **背景色**: 选中状态由原来的半透明色 (`primaryContainer` + alpha) 改为实色主题色 (`MaterialTheme.colorScheme.primary`)。

   * **内容颜色**: 选中状态下的图标和文字颜色由主题色 (`primary`) 改为反色 (`MaterialTheme.colorScheme.onPrimary`)，确保高对比度。

   * 未选中状态保持不变。

2. **修改输入框区域背景色**：

   * 将输入框外层容器 (`Surface`) 的背景色修改为与导航栏一致的颜色。

   * 具体操作是将 `color` 属性统一设置为 `MaterialTheme.colorScheme.surface`，替代原有的 `surfaceVariant` (暗色模式) 和 `LightGrayOpaque` (亮色模式)。这与顶部的 `TopAppBar` 默认背景色保持一致。

